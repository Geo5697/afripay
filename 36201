<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>FreelanceHub — Trouvez les meilleurs talents</title>
<link href="https://fonts.googleapis.com/css2?family=Clash+Display:wght@400;500;600;700&family=Satoshi:wght@300;400;500;700&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0f;
    --surface: #13131a;
    --card: #1a1a25;
    --accent: #6c63ff;
    --accent2: #ff6584;
    --gold: #ffd166;
    --text: #f0f0f8;
    --muted: #8888aa;
    --border: rgba(108,99,255,0.18);
    --radius: 16px;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'Satoshi', sans-serif;
    background: var(--bg);
    color: var(--text);
    overflow-x: hidden;
  }

  /* ── NAV ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 18px 6vw;
    background: rgba(10,10,15,0.85);
    backdrop-filter: blur(18px);
    border-bottom: 1px solid var(--border);
  }

  .logo {
    font-family: 'Clash Display', sans-serif;
    font-size: 1.5rem; font-weight: 700;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    letter-spacing: -0.5px;
  }

  .nav-links { display: flex; gap: 2rem; list-style: none; }
  .nav-links a {
    color: var(--muted); text-decoration: none; font-size: .92rem;
    font-weight: 500; transition: color .2s;
  }
  .nav-links a:hover { color: var(--text); }

  .nav-cta { display: flex; gap: 12px; align-items: center; }

  .btn-outline {
    padding: 9px 22px; border-radius: 50px;
    border: 1px solid var(--border); background: transparent;
    color: var(--text); font-size: .88rem; cursor: pointer;
    transition: all .2s; font-family: 'Satoshi', sans-serif;
  }
  .btn-outline:hover { border-color: var(--accent); color: var(--accent); }

  .btn-primary {
    padding: 10px 24px; border-radius: 50px;
    background: linear-gradient(135deg, var(--accent), #9b59b6);
    border: none; color: #fff; font-size: .88rem;
    cursor: pointer; font-weight: 600;
    transition: transform .2s, box-shadow .2s;
    font-family: 'Satoshi', sans-serif;
    box-shadow: 0 4px 20px rgba(108,99,255,0.3);
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 30px rgba(108,99,255,0.45); }

  .hamburger { display: none; flex-direction: column; gap: 5px; cursor: pointer; }
  .hamburger span { width: 24px; height: 2px; background: var(--text); border-radius: 2px; transition: .3s; }

  /* ── HERO ── */
  .hero {
    min-height: 100vh;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    text-align: center;
    padding: 120px 6vw 80px;
    position: relative; overflow: hidden;
  }

  .hero-bg {
    position: absolute; inset: 0; z-index: 0;
    background:
      radial-gradient(ellipse 80% 60% at 50% 0%, rgba(108,99,255,0.18) 0%, transparent 70%),
      radial-gradient(ellipse 50% 40% at 80% 80%, rgba(255,101,132,0.1) 0%, transparent 60%);
  }

  .hero-badge {
    display: inline-flex; align-items: center; gap: 8px;
    background: rgba(108,99,255,0.12); border: 1px solid var(--border);
    padding: 6px 16px; border-radius: 50px; font-size: .8rem;
    color: var(--accent); margin-bottom: 24px; position: relative; z-index: 1;
    animation: fadeDown .6s ease both;
  }
  .hero-badge::before { content: '✦'; }

  h1 {
    font-family: 'Clash Display', sans-serif;
    font-size: clamp(2.8rem, 7vw, 5.5rem);
    font-weight: 700; line-height: 1.06;
    letter-spacing: -2px;
    position: relative; z-index: 1;
    animation: fadeUp .7s .1s ease both;
  }

  h1 .highlight {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  }

  .hero-sub {
    font-size: clamp(1rem, 2vw, 1.2rem);
    color: var(--muted); max-width: 560px; line-height: 1.7;
    margin: 24px auto 40px;
    position: relative; z-index: 1;
    animation: fadeUp .7s .2s ease both;
  }

  .hero-actions {
    display: flex; gap: 14px; flex-wrap: wrap; justify-content: center;
    position: relative; z-index: 1;
    animation: fadeUp .7s .3s ease both;
  }

  .btn-hero {
    padding: 15px 36px; border-radius: 50px;
    background: linear-gradient(135deg, var(--accent), #9b59b6);
    border: none; color: #fff; font-size: 1rem; font-weight: 700;
    cursor: pointer; transition: transform .2s, box-shadow .2s;
    font-family: 'Satoshi', sans-serif;
    box-shadow: 0 6px 30px rgba(108,99,255,0.4);
  }
  .btn-hero:hover { transform: translateY(-3px); box-shadow: 0 12px 40px rgba(108,99,255,0.5); }

  .btn-hero-ghost {
    padding: 15px 36px; border-radius: 50px;
    background: transparent; border: 1px solid rgba(255,255,255,0.15);
    color: var(--text); font-size: 1rem; font-weight: 600;
    cursor: pointer; transition: all .2s;
    font-family: 'Satoshi', sans-serif;
  }
  .btn-hero-ghost:hover { background: rgba(255,255,255,0.06); border-color: var(--accent); }

  .hero-stats {
    display: flex; gap: 3rem; margin-top: 70px;
    position: relative; z-index: 1;
    animation: fadeUp .7s .4s ease both;
    flex-wrap: wrap; justify-content: center;
  }
  .stat { text-align: center; }
  .stat-num {
    font-family: 'Clash Display', sans-serif;
    font-size: 2rem; font-weight: 700;
    background: linear-gradient(135deg, var(--gold), #ff9f43);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  }
  .stat-label { font-size: .8rem; color: var(--muted); margin-top: 4px; }

  /* ── SEARCH ── */
  .search-bar {
    display: flex; align-items: center;
    background: var(--card); border: 1px solid var(--border);
    border-radius: 50px; padding: 8px 8px 8px 24px;
    max-width: 580px; width: 100%; margin: 0 auto 50px;
    position: relative; z-index: 1;
    animation: fadeUp .7s .25s ease both;
  }
  .search-bar input {
    flex: 1; background: transparent; border: none; outline: none;
    color: var(--text); font-size: .95rem; font-family: 'Satoshi', sans-serif;
  }
  .search-bar input::placeholder { color: var(--muted); }
  .search-bar button {
    padding: 10px 24px; border-radius: 50px;
    background: linear-gradient(135deg, var(--accent), #9b59b6);
    border: none; color: #fff; font-weight: 600; cursor: pointer;
    font-family: 'Satoshi', sans-serif; font-size: .88rem;
    white-space: nowrap;
  }

  /* ── CATEGORIES ── */
  .section { padding: 80px 6vw; }
  .section-title {
    font-family: 'Clash Display', sans-serif;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 700; letter-spacing: -1px;
    margin-bottom: 10px;
  }
  .section-sub { color: var(--muted); margin-bottom: 48px; font-size: .95rem; }

  .cats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(170px, 1fr));
    gap: 16px;
  }

  .cat-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px 20px;
    cursor: pointer;
    transition: transform .25s, border-color .25s, box-shadow .25s;
    position: relative; overflow: hidden;
  }
  .cat-card::before {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    opacity: 0; transition: opacity .25s;
  }
  .cat-card:hover { transform: translateY(-6px); border-color: var(--accent); box-shadow: 0 12px 40px rgba(108,99,255,0.2); }
  .cat-card:hover::before { opacity: 0.06; }

  .cat-icon { font-size: 2rem; margin-bottom: 12px; position: relative; z-index: 1; }
  .cat-name { font-weight: 600; font-size: .95rem; position: relative; z-index: 1; }
  .cat-count { font-size: .78rem; color: var(--muted); margin-top: 4px; position: relative; z-index: 1; }

  /* ── FREELANCERS ── */
  .freelancers-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 24px;
  }

  .freelancer-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px;
    transition: transform .25s, box-shadow .25s;
    cursor: pointer;
  }
  .freelancer-card:hover { transform: translateY(-6px); box-shadow: 0 16px 50px rgba(0,0,0,0.4); }

  .fl-top { display: flex; align-items: center; gap: 14px; margin-bottom: 16px; }
  .fl-avatar {
    width: 52px; height: 52px; border-radius: 50%;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    display: flex; align-items: center; justify-content: center;
    font-size: 1.3rem; font-weight: 700; flex-shrink: 0;
  }
  .fl-name { font-weight: 600; font-size: 1rem; }
  .fl-title { font-size: .8rem; color: var(--muted); margin-top: 2px; }
  .fl-badge {
    margin-left: auto; padding: 3px 10px; border-radius: 50px;
    background: rgba(108,99,255,0.15); color: var(--accent);
    font-size: .72rem; font-weight: 600; flex-shrink: 0;
  }

  .fl-desc { font-size: .85rem; color: var(--muted); line-height: 1.6; margin-bottom: 16px; }

  .fl-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 18px; }
  .tag {
    padding: 4px 12px; border-radius: 50px;
    background: rgba(255,255,255,0.06);
    font-size: .75rem; color: var(--muted);
  }

  .fl-footer { display: flex; align-items: center; justify-content: space-between; }
  .fl-stars { color: var(--gold); font-size: .85rem; }
  .fl-price { font-weight: 700; font-size: 1rem; }
  .fl-price span { font-size: .75rem; color: var(--muted); font-weight: 400; }

  /* ── HOW IT WORKS ── */
  .how-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 30px;
  }
  .how-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 36px 28px;
    position: relative;
  }
  .how-num {
    font-family: 'Clash Display', sans-serif;
    font-size: 3.5rem; font-weight: 700;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    line-height: 1; margin-bottom: 16px;
  }
  .how-title { font-weight: 600; font-size: 1.05rem; margin-bottom: 10px; }
  .how-desc { font-size: .85rem; color: var(--muted); line-height: 1.7; }

  /* ── TESTIMONIALS ── */
  .testi-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 24px;
  }
  .testi-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 30px;
  }
  .testi-stars { color: var(--gold); margin-bottom: 14px; }
  .testi-text { font-size: .9rem; color: var(--muted); line-height: 1.7; margin-bottom: 20px; font-style: italic; }
  .testi-author { display: flex; align-items: center; gap: 12px; }
  .testi-avatar {
    width: 40px; height: 40px; border-radius: 50%;
    background: linear-gradient(135deg, var(--gold), #ff9f43);
    display: flex; align-items: center; justify-content: center;
    font-weight: 700; font-size: .9rem; color: #000;
  }
  .testi-name { font-weight: 600; font-size: .88rem; }
  .testi-role { font-size: .75rem; color: var(--muted); }

  /* ── CTA BANNER ── */
  .cta-banner {
    margin: 0 6vw 80px;
    background: linear-gradient(135deg, rgba(108,99,255,0.2), rgba(155,89,182,0.15));
    border: 1px solid rgba(108,99,255,0.3);
    border-radius: 24px;
    padding: 60px 6vw;
    text-align: center;
    position: relative; overflow: hidden;
  }
  .cta-banner::before {
    content: '';
    position: absolute; top: -50%; left: -50%;
    width: 200%; height: 200%;
    background: radial-gradient(ellipse at center, rgba(108,99,255,0.1) 0%, transparent 60%);
  }
  .cta-banner h2 {
    font-family: 'Clash Display', sans-serif;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 700; letter-spacing: -1px;
    margin-bottom: 14px; position: relative;
  }
  .cta-banner p { color: var(--muted); margin-bottom: 32px; position: relative; }
  .cta-banner .btn-hero { position: relative; }

  /* ── FOOTER ── */
  footer {
    background: var(--surface);
    border-top: 1px solid var(--border);
    padding: 60px 6vw 30px;
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 2fr repeat(3, 1fr);
    gap: 40px; margin-bottom: 50px;
  }
  .footer-brand p { color: var(--muted); font-size: .88rem; line-height: 1.7; margin-top: 12px; max-width: 280px; }
  .footer-col h4 { font-weight: 600; margin-bottom: 16px; font-size: .9rem; }
  .footer-col ul { list-style: none; }
  .footer-col li { margin-bottom: 10px; }
  .footer-col a { color: var(--muted); text-decoration: none; font-size: .85rem; transition: color .2s; }
  .footer-col a:hover { color: var(--text); }
  .footer-bottom {
    border-top: 1px solid var(--border);
    padding-top: 24px;
    display: flex; justify-content: space-between; align-items: center;
    flex-wrap: wrap; gap: 12px;
  }
  .footer-bottom p { font-size: .8rem; color: var(--muted); }

  /* ── MODALS ── */
  .modal-overlay {
    position: fixed; inset: 0; z-index: 1000;
    background: rgba(0,0,0,0.75); backdrop-filter: blur(8px);
    display: flex; align-items: center; justify-content: center;
    padding: 20px; opacity: 0; pointer-events: none; transition: opacity .3s;
  }
  .modal-overlay.open { opacity: 1; pointer-events: all; }

  .modal {
    background: var(--card); border: 1px solid var(--border);
    border-radius: 24px; padding: 40px; width: 100%; max-width: 460px;
    max-height: 90vh; overflow-y: auto;
    transform: translateY(30px); transition: transform .3s;
  }
  .modal-overlay.open .modal { transform: translateY(0); }

  .modal h2 {
    font-family: 'Clash Display', sans-serif;
    font-size: 1.8rem; font-weight: 700; margin-bottom: 6px;
  }
  .modal p.sub { color: var(--muted); font-size: .9rem; margin-bottom: 28px; }

  .form-group { margin-bottom: 18px; }
  .form-group label { display: block; font-size: .85rem; font-weight: 500; margin-bottom: 8px; }
  .form-group input, .form-group select, .form-group textarea {
    width: 100%; padding: 13px 16px;
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 12px; color: var(--text);
    font-family: 'Satoshi', sans-serif; font-size: .9rem;
    outline: none; transition: border-color .2s;
  }
  .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
    border-color: var(--accent);
  }
  .form-group select option { background: var(--surface); }

  .phone-row { display: flex; gap: 10px; }
  .phone-row select { width: 140px; flex-shrink: 0; }
  .phone-row input { flex: 1; }

  .form-check { display: flex; align-items: flex-start; gap: 10px; margin-bottom: 24px; }
  .form-check input[type=checkbox] { margin-top: 3px; accent-color: var(--accent); }
  .form-check label { font-size: .83rem; color: var(--muted); line-height: 1.5; }

  .modal-footer { margin-top: 10px; text-align: center; font-size: .85rem; color: var(--muted); }
  .modal-footer a { color: var(--accent); cursor: pointer; text-decoration: none; }

  .close-modal {
    position: absolute; top: 16px; right: 20px;
    background: none; border: none; color: var(--muted);
    font-size: 1.4rem; cursor: pointer; line-height: 1;
  }
  .modal { position: relative; }

  .divider { display: flex; align-items: center; gap: 12px; margin: 20px 0; }
  .divider::before, .divider::after { content: ''; flex: 1; height: 1px; background: var(--border); }
  .divider span { font-size: .8rem; color: var(--muted); }

  /* ── TOAST ── */
  .toast {
    position: fixed; bottom: 24px; right: 24px; z-index: 9999;
    background: var(--card); border: 1px solid var(--accent);
    border-radius: 12px; padding: 14px 20px;
    font-size: .9rem; color: var(--text);
    transform: translateY(80px); opacity: 0;
    transition: all .3s; pointer-events: none;
    max-width: 320px;
  }
  .toast.show { transform: translateY(0); opacity: 1; }

  /* ── MOBILE MENU ── */
  .mobile-menu {
    position: fixed; top: 70px; left: 0; right: 0; z-index: 99;
    background: var(--surface); border-bottom: 1px solid var(--border);
    padding: 20px 6vw;
    display: none; flex-direction: column; gap: 16px;
  }
  .mobile-menu.open { display: flex; }
  .mobile-menu a { color: var(--muted); text-decoration: none; font-size: .95rem; font-weight: 500; }
  .mobile-menu a:hover { color: var(--text); }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeDown {
    from { opacity: 0; transform: translateY(-12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ── RESPONSIVE ── */
  @media (max-width: 768px) {
    .nav-links, .nav-cta { display: none; }
    .hamburger { display: flex; }
    .footer-grid { grid-template-columns: 1fr 1fr; }
    .hero-stats { gap: 1.5rem; }
  }
  @media (max-width: 480px) {
    .footer-grid { grid-template-columns: 1fr; }
    .modal { padding: 28px 20px; }
    .hero-actions { flex-direction: column; align-items: center; }
  }

  /* Scrollbar */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo">FreelanceHub</div>
  <ul class="nav-links">
    <li><a href="#categories">Catégories</a></li>
    <li><a href="#talents">Talents</a></li>
    <li><a href="#comment">Comment ça marche</a></li>
    <li><a href="#avis">Avis</a></li>
  </ul>
  <div class="nav-cta">
    <button class="btn-outline" onclick="openModal('login')">Se connecter</button>
    <button class="btn-primary" onclick="openModal('register')">S'inscrire</button>
  </div>
  <div class="hamburger" onclick="toggleMenu()">
    <span></span><span></span><span></span>
  </div>
</nav>

<!-- MOBILE MENU -->
<div class="mobile-menu" id="mobileMenu">
  <a href="#categories" onclick="toggleMenu()">Catégories</a>
  <a href="#talents" onclick="toggleMenu()">Talents</a>
  <a href="#comment" onclick="toggleMenu()">Comment ça marche</a>
  <a href="#avis" onclick="toggleMenu()">Avis</a>
  <button class="btn-outline" onclick="toggleMenu(); openModal('login')">Se connecter</button>
  <button class="btn-primary" onclick="toggleMenu(); openModal('register')">S'inscrire</button>
</div>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-badge">Plateforme de talents en ligne #1</div>
  <h1>Trouvez les <span class="highlight">meilleurs freelances</span><br>pour vos projets</h1>
  <p class="hero-sub">Des milliers de professionnels qualifiés — design, développement, rédaction, marketing — prêts à transformer vos idées en réalité.</p>

  <div class="search-bar">
    <input type="text" placeholder="Rechercher un service (ex: logo, site web, traduction...)">
    <button>Rechercher</button>
  </div>

  <div class="hero-actions">
    <button class="btn-hero" onclick="openModal('register')">Trouver un freelance</button>
    <button class="btn-hero-ghost" onclick="openModal('register-pro')">Devenir freelance</button>
  </div>

  <div class="hero-stats">
    <div class="stat"><div class="stat-num">12 000+</div><div class="stat-label">Freelances actifs</div></div>
    <div class="stat"><div class="stat-num">48 000+</div><div class="stat-label">Projets réalisés</div></div>
    <div class="stat"><div class="stat-num">4.9 ★</div><div class="stat-label">Note moyenne</div></div>
    <div class="stat"><div class="stat-num">98%</div><div class="stat-label">Clients satisfaits</div></div>
  </div>
</section>

<!-- CATEGORIES -->
<section class="section" id="categories">
  <div class="section-title">Explorez par catégorie</div>
  <div class="section-sub">Des compétences pour chaque besoin</div>
  <div class="cats-grid">
    <div class="cat-card"><div class="cat-icon">🎨</div><div class="cat-name">Design & Créatif</div><div class="cat-count">2 340 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">💻</div><div class="cat-name">Développement</div><div class="cat-count">3 120 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">✍️</div><div class="cat-name">Rédaction</div><div class="cat-count">1 890 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">📱</div><div class="cat-name">Marketing Digital</div><div class="cat-count">1 560 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">🎬</div><div class="cat-name">Vidéo & Animation</div><div class="cat-count">980 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">🌍</div><div class="cat-name">Traduction</div><div class="cat-count">760 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">📊</div><div class="cat-name">Business & Finance</div><div class="cat-count">640 freelances</div></div>
    <div class="cat-card"><div class="cat-icon">🎵</div><div class="cat-name">Audio & Musique</div><div class="cat-count">420 freelances</div></div>
  </div>
</section>

<!-- TOP FREELANCERS -->
<section class="section" id="talents" style="padding-top:0">
  <div class="section-title">Talents en vedette</div>
  <div class="section-sub">Des professionnels vérifiés, prêts à travailler</div>
  <div class="freelancers-grid">

    <div class="freelancer-card" onclick="openModal('register')">
      <div class="fl-top">
        <div class="fl-avatar">AK</div>
        <div>
          <div class="fl-name">Ama Koffi</div>
          <div class="fl-title">UI/UX Designer Senior</div>
        </div>
        <div class="fl-badge">Pro</div>
      </div>
      <div class="fl-desc">Spécialiste en design d'interfaces mobiles et web avec 6 ans d'expérience sur des projets internationaux.</div>
      <div class="fl-tags">
        <span class="tag">Figma</span><span class="tag">UI Design</span><span class="tag">Prototypage</span>
      </div>
      <div class="fl-footer">
        <div class="fl-stars">★★★★★ <span style="color:var(--muted)">5.0 (124)</span></div>
        <div class="fl-price">25 000 FCFA <span>/ projet</span></div>
      </div>
    </div>

    <div class="freelancer-card" onclick="openModal('register')">
      <div class="fl-top">
        <div class="fl-avatar" style="background:linear-gradient(135deg,#11998e,#38ef7d)">MB</div>
        <div>
          <div class="fl-name">Moussa Bah</div>
          <div class="fl-title">Développeur Full Stack</div>
        </div>
        <div class="fl-badge">Pro</div>
      </div>
      <div class="fl-desc">Expert React, Node.js et bases de données. Je construis des applications web performantes et évolutives.</div>
      <div class="fl-tags">
        <span class="tag">React</span><span class="tag">Node.js</span><span class="tag">MySQL</span>
      </div>
      <div class="fl-footer">
        <div class="fl-stars">★★★★★ <span style="color:var(--muted)">4.9 (87)</span></div>
        <div class="fl-price">50 000 FCFA <span>/ projet</span></div>
      </div>
    </div>

    <div class="freelancer-card" onclick="openModal('register')">
      <div class="fl-top">
        <div class="fl-avatar" style="background:linear-gradient(135deg,#f7971e,#ffd200)">FD</div>
        <div>
          <div class="fl-name">Fatou Diallo</div>
          <div class="fl-title">Rédactrice & Traductrice</div>
        </div>
        <div class="fl-badge">Top</div>
      </div>
      <div class="fl-desc">Rédactrice web SEO bilingue français/anglais. Contenus optimisés qui convertissent et fidélisent.</div>
      <div class="fl-tags">
        <span class="tag">SEO</span><span class="tag">Copywriting</span><span class="tag">Traduction</span>
      </div>
      <div class="fl-footer">
        <div class="fl-stars">★★★★★ <span style="color:var(--muted)">4.8 (203)</span></div>
        <div class="fl-price">15 000 FCFA <span>/ article</span></div>
      </div>
    </div>

    <div class="freelancer-card" onclick="openModal('register')">
      <div class="fl-top">
        <div class="fl-avatar" style="background:linear-gradient(135deg,#cc2b5e,#753a88)">KT</div>
        <div>
          <div class="fl-name">Kofi Tetteh</div>
          <div class="fl-title">Social Media Manager</div>
        </div>
        <div class="fl-badge">Top</div>
      </div>
      <div class="fl-desc">Je gère vos réseaux sociaux et booste votre présence en ligne. Stratégie, contenu et publicités ciblées.</div>
      <div class="fl-tags">
        <span class="tag">Instagram</span><span class="tag">TikTok</span><span class="tag">Publicité</span>
      </div>
      <div class="fl-footer">
        <div class="fl-stars">★★★★☆ <span style="color:var(--muted)">4.7 (156)</span></div>
        <div class="fl-price">35 000 FCFA <span>/ mois</span></div>
      </div>
    </div>

  </div>
</section>

<!-- HOW IT WORKS -->
<section class="section" id="comment" style="background:var(--surface)">
  <div class="section-title">Comment ça marche ?</div>
  <div class="section-sub">Simple, rapide et sécurisé</div>
  <div class="how-grid">
    <div class="how-card"><div class="how-num">01</div><div class="how-title">Créez votre compte</div><div class="how-desc">Inscrivez-vous gratuitement en quelques secondes. Aucune carte bancaire requise.</div></div>
    <div class="how-card"><div class="how-num">02</div><div class="how-title">Trouvez un talent</div><div class="how-desc">Parcourez des milliers de profils vérifiés et filtrez par compétence, budget ou note.</div></div>
    <div class="how-card"><div class="how-num">03</div><div class="how-title">Passez commande</div><div class="how-desc">Discutez des détails avec le freelance et finalisez votre commande en toute sécurité.</div></div>
    <div class="how-card"><div class="how-num">04</div><div class="how-title">Recevez votre travail</div><div class="how-desc">Le paiement n'est libéré que lorsque vous êtes 100% satisfait du livrable.</div></div>
  </div>
</section>

<!-- TESTIMONIALS -->
<section class="section" id="avis">
  <div class="section-title">Ce que disent nos clients</div>
  <div class="section-sub">Des milliers de projets réussis</div>
  <div class="testi-grid">
    <div class="testi-card">
      <div class="testi-stars">★★★★★</div>
      <div class="testi-text">"J'ai trouvé un développeur exceptionnel en moins de 24h. Mon site e-commerce est maintenant en ligne et les ventes explosent !"</div>
      <div class="testi-author">
        <div class="testi-avatar">SB</div>
        <div><div class="testi-name">Seydou Bakayoko</div><div class="testi-role">Entrepreneur, Abidjan</div></div>
      </div>
    </div>
    <div class="testi-card">
      <div class="testi-stars">★★★★★</div>
      <div class="testi-text">"La qualité des freelances est incroyable. Ma logo a été livrée en 2 jours, exactement comme je la voulais. Je recommande !"</div>
      <div class="testi-author">
        <div class="testi-avatar" style="background:linear-gradient(135deg,#11998e,#38ef7d)">MK</div>
        <div><div class="testi-name">Marie Kaboré</div><div class="testi-role">Directrice, Ouagadougou</div></div>
      </div>
    </div>
    <div class="testi-card">
      <div class="testi-stars">★★★★★</div>
      <div class="testi-text">"Plateforme sérieuse et fiable. Le système de paiement sécurisé me donne confiance. Utilisateur depuis 2 ans."</div>
      <div class="testi-author">
        <div class="testi-avatar" style="background:linear-gradient(135deg,#cc2b5e,#753a88)">OD</div>
        <div><div class="testi-name">Omar Diallo</div><div class="testi-role">Chef de projet, Dakar</div></div>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<div class="cta-banner">
  <h2>Prêt à démarrer votre projet ?</h2>
  <p>Rejoignez plus de 48 000 clients satisfaits. Inscription gratuite, sans engagement.</p>
  <button class="btn-hero" onclick="openModal('register')">Commencer maintenant — c'est gratuit</button>
</div>

<!-- FOOTER -->
<footer>
  <div class="footer-grid">
    <div class="footer-brand">
      <div class="logo">FreelanceHub</div>
      <p>La plateforme de référence pour connecter les entreprises et les meilleurs talents freelance d'Afrique et du monde.</p>
    </div>
    <div class="footer-col">
      <h4>Plateforme</h4>
      <ul>
        <li><a href="#">Trouver un freelance</a></li>
        <li><a href="#">Devenir freelance</a></li>
        <li><a href="#">Catégories</a></li>
        <li><a href="#">Tarifs</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Assistance</h4>
      <ul>
        <li><a href="#">Centre d'aide</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#">Sécurité</a></li>
        <li><a href="#">Signaler un abus</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Légal</h4>
      <ul>
        <li><a href="#">Conditions d'utilisation</a></li>
        <li><a href="#">Politique de confidentialité</a></li>
        <li><a href="#">Mentions légales</a></li>
        <li><a href="#">Cookies</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p>© 2026 FreelanceHub. Tous droits réservés.</p>
    <p>🌍 Afrique de l'Ouest · 🔒 Paiements sécurisés</p>
  </div>
</footer>

<!-- MODAL: REGISTER CLIENT -->
<div class="modal-overlay" id="modal-register">
  <div class="modal">
    <button class="close-modal" onclick="closeModal('register')">✕</button>
    <h2>Créer un compte</h2>
    <p class="sub">Trouvez les meilleurs freelances pour vos projets</p>
    <form onsubmit="handleRegister(event)">
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
        <div class="form-group">
          <label>Prénom *</label>
          <input type="text" placeholder="Jean" required>
        </div>
        <div class="form-group">
          <label>Nom *</label>
          <input type="text" placeholder="Dupont" required>
        </div>
      </div>
      <div class="form-group">
        <label>Email *</label>
        <input type="email" placeholder="jean@exemple.com" required>
      </div>
      <div class="form-group">
        <label>Numéro de téléphone *</label>
        <div class="phone-row">
          <select>
            <option value="+229">🇧🇯 +229 Bénin</option>
            <option value="+228">🇹🇬 +228 Togo</option>
            <option value="+225">🇨🇮 +225 Côte d'Ivoire</option>
            <option value="+221">🇸🇳 +221 Sénégal</option>
            <option value="+226">🇧🇫 +226 Burkina Faso</option>
            <option value="+223">🇲🇱 +223 Mali</option>
            <option value="+227">🇳🇪 +227 Niger</option>
            <option value="+237">🇨🇲 +237 Cameroun</option>
            <option value="+242">🇨🇬 +242 Congo</option>
            <option value="+241">🇬🇦 +241 Gabon</option>
            <option value="+235">🇹🇩 +235 Tchad</option>
          </select>
          <input type="tel" placeholder="XX XX XX XX XX" required>
        </div>
      </div>
      <div class="form-group">
        <label>Mot de passe *</label>
        <input type="password" placeholder="Minimum 8 caractères" required minlength="8">
      </div>
      <div class="form-check">
        <input type="checkbox" id="terms1" required>
        <label for="terms1">J'accepte les <a href="#" style="color:var(--accent)">conditions d'utilisation</a> et la <a href="#" style="color:var(--accent)">politique de confidentialité</a></label>
      </div>
      <button type="submit" class="btn-primary" style="width:100%;padding:14px;font-size:1rem;border-radius:12px">Créer mon compte</button>
    </form>
    <div class="divider"><span>ou</span></div>
    <div class="modal-footer">Déjà un compte ? <a onclick="switchModal('register','login')">Se connecter</a></div>
  </div>
</div>

<!-- MODAL: LOGIN -->
<div class="modal-overlay" id="modal-login">
  <div class="modal">
    <button class="close-modal" onclick="closeModal('login')">✕</button>
    <h2>Se connecter</h2>
    <p class="sub">Bon retour parmi nous !</p>
    <form onsubmit="handleLogin(event)">
      <div class="form-group">
        <label>Email *</label>
        <input type="email" placeholder="jean@exemple.com" required>
      </div>
      <div class="form-group">
        <label>Mot de passe *</label>
        <input type="password" placeholder="Votre mot de passe" required>
      </div>
      <div style="text-align:right;margin-bottom:20px">
        <a href="#" style="font-size:.83rem;color:var(--accent)">Mot de passe oublié ?</a>
      </div>
      <button type="submit" class="btn-primary" style="width:100%;padding:14px;font-size:1rem;border-radius:12px">Se connecter</button>
    </form>
    <div class="divider"><span>ou</span></div>
    <div class="modal-footer">Pas encore de compte ? <a onclick="switchModal('login','register')">S'inscrire gratuitement</a></div>
  </div>
</div>

<!-- MODAL: REGISTER PRO -->
<div class="modal-overlay" id="modal-register-pro">
  <div class="modal">
    <button class="close-modal" onclick="closeModal('register-pro')">✕</button>
    <h2>Devenir Freelance</h2>
    <p class="sub">Proposez vos services et trouvez des clients</p>
    <form onsubmit="handleRegisterPro(event)">
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
        <div class="form-group">
          <label>Prénom *</label>
          <input type="text" required placeholder="Votre prénom">
        </div>
        <div class="form-group">
          <label>Nom *</label>
          <input type="text" required placeholder="Votre nom">
        </div>
      </div>
      <div class="form-group">
        <label>Email *</label>
        <input type="email" required placeholder="votre@email.com">
      </div>
      <div class="form-group">
        <label>Catégorie principale *</label>
        <select required>
          <option value="">Choisir une catégorie</option>
          <option>Design & Créatif</option>
          <option>Développement Web/Mobile</option>
          <option>Rédaction & Traduction</option>
          <option>Marketing Digital</option>
          <option>Vidéo & Animation</option>
          <option>Audio & Musique</option>
          <option>Business & Finance</option>
        </select>
      </div>
      <div class="form-group">
        <label>Numéro de téléphone *</label>
        <div class="phone-row">
          <select>
            <option value="+229">🇧🇯 +229</option>
            <option value="+228">🇹🇬 +228</option>
            <option value="+225">🇨🇮 +225</option>
            <option value="+221">🇸🇳 +221</option>
            <option value="+226">🇧🇫 +226</option>
            <option value="+223">🇲🇱 +223</option>
            <option value="+227">🇳🇪 +227</option>
            <option value="+237">🇨🇲 +237</option>
            <option value="+242">🇨🇬 +242</option>
            <option value="+241">🇬🇦 +241</option>
            <option value="+235">🇹🇩 +235</option>
          </select>
          <input type="tel" placeholder="Numéro" required>
        </div>
      </div>
      <div class="form-group">
        <label>Mot de passe *</label>
        <input type="password" required placeholder="Minimum 8 caractères" minlength="8">
      </div>
      <div class="form-check">
        <input type="checkbox" id="terms2" required>
        <label for="terms2">J'accepte les <a href="#" style="color:var(--accent)">conditions d'utilisation</a></label>
      </div>
      <button type="submit" class="btn-primary" style="width:100%;padding:14px;font-size:1rem;border-radius:12px">Créer mon profil freelance</button>
    </form>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
  function openModal(id) {
    document.getElementById('modal-' + id).classList.add('open');
    document.body.style.overflow = 'hidden';
  }
  function closeModal(id) {
    document.getElementById('modal-' + id).classList.remove('open');
    document.body.style.overflow = '';
  }
  function switchModal(from, to) {
    closeModal(from);
    setTimeout(() => openModal(to), 200);
  }

  // Close on overlay click
  document.querySelectorAll('.modal-overlay').forEach(el => {
    el.addEventListener('click', function(e) {
      if (e.target === this) {
        this.classList.remove('open');
        document.body.style.overflow = '';
      }
    });
  });

  function showToast(msg) {
    const t = document.getElementById('toast');
    t.textContent = msg;
    t.classList.add('show');
    setTimeout(() => t.classList.remove('show'), 3500);
  }

  function handleRegister(e) {
    e.preventDefault();
    closeModal('register');
    showToast('✅ Compte créé avec succès ! Bienvenue sur FreelanceHub.');
  }
  function handleLogin(e) {
    e.preventDefault();
    closeModal('login');
    showToast('✅ Connexion réussie ! Bon retour parmi nous.');
  }
  function handleRegisterPro(e) {
    e.preventDefault();
    closeModal('register-pro');
    showToast('🎉 Profil freelance créé ! Complétez votre profil pour attirer des clients.');
  }

  function toggleMenu() {
    document.getElementById('mobileMenu').classList.toggle('open');
  }

  // Smooth scroll
  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', e => {
      const target = document.querySelector(a.getAttribute('href'));
      if (target) { e.preventDefault(); target.scrollIntoView({ behavior: 'smooth' }); }
    });
  });

  // Search bar
  document.querySelector('.search-bar button').addEventListener('click', function() {
    const val = document.querySelector('.search-bar input').value.trim();
    if (val) showToast(`🔍 Recherche de "${val}"...`);
    else showToast('Entrez un mot-clé pour rechercher.');
  });
  document.querySelector('.search-bar input').addEventListener('keydown', function(e) {
    if (e.key === 'Enter') document.querySelector('.search-bar button').click();
  });
</script>
</body>
</html>
